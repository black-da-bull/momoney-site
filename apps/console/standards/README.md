# House standards

Operator-set laws loaded into every session's system prompt, after the soul.

- `lyric-quality-baseline.md` — the LYRIC QUALITY BASELINE ("the anti-corny firewall").
  Extracted **verbatim** from the operator's own session input (project
  `not-like-them`, 2026-07-29) — not authored, not paraphrased. The operator's text is
  the law; edits to it are operator acts.

Files here are alphabetical-concatenated by `src/lib/standards.ts`. To add a standard,
drop a `.md` here — verbatim operator text only.

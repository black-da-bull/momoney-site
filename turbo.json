{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "DATABASE_URL",
    "POSTGRES_URL",
    "PRISMA_DATABASE_URL",
    "storage_DATABASE_URL",
    "storage_POSTGRES_URL",
    "storage_PRISMA_DATABASE_URL",
    "ANTHROPIC_API_KEY",
    "MAESTRO_MODEL",
    "MAESTRO_EXTRACT_MODEL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {}
  }
}
